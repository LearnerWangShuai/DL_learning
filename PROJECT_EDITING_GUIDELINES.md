# 项目编辑条例

**状态：** 生效中  
**最后更新：** 2026-09-25

本文件记录本项目的编辑约定。后续新增规则请直接补充到相应章节，并更新“最后更新”日期。

## 1. 命名与目录

- 所有文件夹必须使用英文小写；多词之间使用下划线，例如 `reinforcement_learning_intro`。
- Python 源文件使用英文小写与下划线，例如 `q_learning.py`。
- Markdown 文档使用英文大写文件名与下划线，例如 `PROJECT_OVERVIEW.md`。
- 代码中的变量、函数、类和命令行参数使用英文；面向学习者的说明文字可使用中文。

## 2. 代码变更

- 一个模块只负责一个清晰职责；环境、智能体、训练与可视化应尽量解耦。
- 新增环境时，必须实现 `rl_platform/environments/base.py` 中的统一接口，并在环境注册表中登记；奖励逻辑应放入 `rl_platform/rewards/`，不得混入智能体代码。
- 新增算法时，必须实现 `rl_platform/agents/base.py` 中的统一接口，并在智能体注册表中登记；算法专属超参数放入 `config.json` 的 `agent.settings`，通用训练循环参数放入 `training`。
- 添加功能时优先保留可直接运行的最小示例，并为关键参数提供默认值和说明。
- 训练超参数、环境地图和奖励规则等可调默认值必须放在子项目的 `config.json`；不要重新写死在 Python 代码中。
- 不提交密钥、账号信息、个人截图、训练缓存、模型权重或其他大型生成文件。
- 新增第三方依赖前，应在文档说明用途、安装方式和兼容的 Python 版本。

## 3. 运行环境

- 本项目必须使用独立 Conda 环境：`D:\Anaconda\envs\rl_learning_project`。
- 禁止将本项目的新依赖安装到 Anaconda 的 Base 环境；所有包必须安装到上述独立环境。
- 在 Windows PowerShell 中运行项目时，直接调用专用环境的 `python.exe`；不要使用 `conda run`，以免旧版 PowerShell 的 GBK 代码页与其 UTF-8 转发输出发生冲突并显示乱码。
- 迷宫可视化使用 `pygame-ce`，在代码中仍通过 `import pygame` 导入。
- 新增或升级依赖时，必须同步更新根目录的 `requirements.txt`，并在独立环境中执行安装。
- 运行项目时优先在项目根目录执行命令；避免依赖未记录的全局路径或 IDE 专属设置。

## 4. 验证要求

- 修改 Python 文件后，至少执行一次语法检查或对应的最小运行测试。
- 修改训练逻辑后，确认智能体能完成一次有效演示，并记录核心结果。
- 修改可视化后，确认窗口能够打开、关闭，并能显示状态与每一步动作。
- 修改文档中的命令或路径后，检查其与实际目录一致。

## 5. 文档维护

- 项目目标、目录结构或运行方式变化时，同步更新 `PROJECT_OVERVIEW.md`。
- 重要设计选择、限制和未解决问题应写入项目说明，而不是只留在聊天记录中。

## 6. 待补充规则

在此处持续添加新的编辑条例：

- （待补充）
